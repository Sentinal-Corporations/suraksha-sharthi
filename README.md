# Suraksha Parchi — live app (gh-pages branch)

This branch serves the published app. Source of truth lives on `main` under `mvp/`.

- App entry: `index.html`
- Data: `data/` (49 medicines, interactions, Jan Aushadhi, Ayushman, helplines)
- On-device AI weights: `engine/needle2.cact`
